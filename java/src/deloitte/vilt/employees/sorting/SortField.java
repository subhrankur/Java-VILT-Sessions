/*
 * TODO:
 * Create an enum representing the fields by which employees can be sorted.
 *
 * Suggested values:
 * - ID
 * - NAME
 * - DEPARTMENT
 * - SALARY
 * - EXPERIENCE
 *
 * This enum must be used instead of string-based sorting requests.
 */

package deloitte.vilt.employees.sorting;

public enum SortField {
    ID,
    NAME,
    DEPARTMENT,
    SALARY,
    EXPERIENCE
}